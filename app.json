{
  "name": "Telegram GPT Bot",
  "description": "Simple Telegram bot with OpenAI GPT deployed on Heroku",
  "env": {
    "TELEGRAM_TOKEN": {
      "description": "Telegram bot token from BotFather",
      "required": true
    },
    "OPENAI_API_KEY": {
      "description": "Your OpenAI API key",
      "required": true
    }
  }
}
